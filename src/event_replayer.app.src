{application, event_replayer,
 [
  {description, "requests replayer"},
  {vsn, "0.1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { event_replayer_app, []}},
  {env, [
    {worker_pool_size, 5},
    {disk_log, [{pattern, ["users_update_requests.*.log", "submit_requests.*.log"]}]},
    {csv, [{pattern, ["search_requests.*.log"]}]}
  ]}
 ]}.
