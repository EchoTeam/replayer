{application, event_replayer,
 [
  {description, "requests replayer"},
  {vsn, "0.1"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib
                 ]},
  {mod, { event_replayer_app, []}},
  {env, [
    {worker_pool_size, 5},
    {log_processors, [
        {disk_log, disk_log_processor},
        {csv_log,  csv_log_processor},
        {http_log, http_log_processor} ]}
  ]}
 ]}.
